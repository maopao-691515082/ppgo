class InternalExc
{
    s string;

    func init(s string)
    {
        this.s = s;
    }

    public func str() string
    {
        return this.s;
    }
}

public final var (
    kExcTimeout     any = InternalExc("timeout");
    kExcClosed      any = InternalExc("closed");
    kExcCanceled    any = InternalExc("canceled");
    kExcConnReset   any = InternalExc("connect reset by peer");
)
